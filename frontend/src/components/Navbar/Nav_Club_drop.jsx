import React from 'react'
import {navClubsItems} from '../../data/Navbar_data'

export default function Nav_Club_dropdowns() {
return (
    <div className='Nav_panel_dropdowns'>
        {/* {
            navClubsItems.map((items) =>(
                <a href={items.path}>
                    <div className="Nav_panel_clubs_items" tabIndex={items.index}>
                        {"Test"}
                    </div>
                </a>
            ))
        } */}
    </div>
)
}
